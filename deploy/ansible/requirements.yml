---
# ansible-galaxy collection install -r requirements.yml
collections:
  - name: amazon.aws
    version: ">=8.0.0"
  - name: community.crypto
    version: ">=2.0.0"
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
